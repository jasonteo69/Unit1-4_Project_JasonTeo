public class Race
    initializes private variables:
    -int score, lower, upper, incorrect
    -private int random1, random2
    -private String sign


public Race (int1, int2, int3, int4) {
    set score, upper, lower, incorrect to int 1,2,3,4 respectively
}

String pickCar(int randomNumber) {
depending on randomNumber, a string of a description of a car will be returned
}

void countdown() {
adds a visual effect, displays a 3 second countdown
}
firstNumber {
    math.random limits of upper and lower
}

secondNumber {
math.random limits of upper and lower
}

correct method () {
if answer to problem given by equation method is correct, return true
{

method retreat (int subtracted) {
subtract points
returns score - subtracted
}

method void youSmart {
add one to score.
}

String toString {
displays results of the game
}

string equation {
gives an equation with a -, +, or x sign
}


string randomCar {
return result of car 1,2,3,4 based on a math.random choice
}


